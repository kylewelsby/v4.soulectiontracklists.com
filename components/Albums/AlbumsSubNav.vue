<template lang="pug">
  SiteSubNav(
    title="Records"
  )
    SiteSubNavLink(
      to="/records/"
    ) All Releases
    SiteSubNavLink(
      to="/records/t/black-label"
    ) Black Label
    SiteSubNavLink(
      to="/records/t/white-label"
    ) White Label
    SiteSubNavLink(
      to="/records/t/singles"
    ) Singles
    SiteSubNavLink(
      to="/records/t/compilations"
    ) Compilations
</template>
